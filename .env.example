# Service layer target.
# Empty  -> every request() call falls back to the bundled demo dataset.
# Set    -> the same calls hit a real backend; no component changes needed.
VITE_API_BASE_URL=

# Active tenant. Selects the brand config in src/brand/brand.config.js.
# vinted | priceline
VITE_TENANT=vinted
